"use client";

/**
 * @deprecated Import from `@/hooks/useIdentityAlignment`.
 * Re-export kept so existing call sites keep working.
 */
export {
  useCompassAlignment,
  useIdentityAlignment,
} from "@/hooks/useIdentityAlignment";
