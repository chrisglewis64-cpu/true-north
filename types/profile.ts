export type Profile = {
  id: string;
  email: string;
  displayName: string;
  timezone: string;
  onboardingComplete: boolean;
  createdAt: string;
  updatedAt: string;
};

export type ProfileRow = {
  id: string;
  email: string;
  display_name: string;
  timezone: string;
  onboarding_complete: boolean;
  created_at: string;
  updated_at: string;
};
