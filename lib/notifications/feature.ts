/**
 * Flip to `true` to restore notification preferences, permission,
 * push registration, and Settings interactivity without redesigning the UI.
 */
export const NOTIFICATIONS_FEATURE_ENABLED = false;
